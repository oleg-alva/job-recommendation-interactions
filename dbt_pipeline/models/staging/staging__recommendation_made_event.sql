SELECT 
    id, -- string
    created_at, -- timestamp
    user_id, -- string
    job_position_id, -- string
    status, -- string
FROM `alva-recommendations.recommendations_events_production.job_recommendation_created`